Read Alvaro/CLAUDE.md and Alvaro/src/make_submission.py. Work ONLY inside Alvaro/.
This is a READ-ONLY diagnostic — no submission, no registration, no long run.

HYPOTHESIS: the test data contains a deterministic metadata leak (NIfTI header
geometry and/or header text fields) that identifies matching query<->target pairs
without image content. Three teams are at exactly 1.00000, which content methods
don't produce — we want to find what they found.

Build Alvaro/src/leak_probe.py that does THREE things and prints findings:

1) LABELED DISCOVERY (dataset1 train pairs, which we have ground truth for):
   For ~50 true (query,target) pairs, load BOTH NIfTI headers (nibabel) and compare
   field by field: shape, affine matrix (np.allclose), pixdim/zooms, qform/sform
   codes, qoffset, srow_*, and the text fields descrip, aux_file, db_name,
   intent_name, plus any header.extensions. Report, per field: (a) does it MATCH
   within true pairs, and (b) is it DISCRIMINATIVE — i.e. does it NOT match for
   randomly paired wrong (query,target)? A field that is pair-identical AND
   cross-pair-distinct is a leak. Print a ranked list of leak candidates.

2) STRUCTURAL CHECK on ds3 and ds2 VAL (unlabeled): build the
   queries x targets matrix of affine-matrix distance (Frobenius norm of
   affine_q - affine_t) and shape-equality. Report: for each query, the smallest
   affine distance and the gap to the 2nd smallest; and whether argmin-per-query
   forms a clean BIJECTION (each target chosen exactly once). A clean bijection with
   near-zero best distances and a large gap to 2nd = the geometry leak is real on
   that dataset, even without labels.

3) RAW DUMP: for 3 ds3 val queries, print the full header text fields and affine of
   the query and its 3 nearest-affine targets, so I can eyeball any embedded ID.

Print a clear verdict per dataset: "geometry leak: yes/no", "text-field leak:
yes/no", and which field(s). Do NOT build a ranker or submit yet — first we confirm
what the signal is.

---

Read Alvaro/src/leak_probe.py, Alvaro/src/make_submission.py, Alvaro/src/reg_mi_ranker.py.
Work ONLY inside Alvaro/.

TWO tasks.

TASK A (fast, do first) — wire the confirmed ds3 affine leak:
- Add an affine-argmin ranker for ds3: for each query, rank gallery targets by
  ASCENDING Frobenius distance between query and target affine matrices (the exact
  signal leak_probe found: perfect bijection, ~1e-9 best distance). No image loading
  beyond headers; runs in seconds.
- Extend the ranker-map so a combined run uses: ds1 plain_mi, ds2 reg_mi affine,
  ds3 affine_argmin.
- Generate Alvaro/generated/leak_ds3_val.csv (ds3 VAL only) so I can submit it in
  isolation and confirm it scores ~0.333 (=1.0/3). Print a sanity line: is the
  argmin a clean bijection (each target used once)? If not, warn me.
- Do NOT regenerate the full submission yet — I'll confirm the ds3 val score first.

TASK B (the ds2 lever) — measure SyN cost + quality on ds2-val ONLY:
- Run reg-MI with --transform SyN on dataset2 VAL (40x40 = 1600 SyN registrations).
- FIRST print an upfront cost estimate: probe ONE SyN registration at grid 96, print
  its wall-time, multiply by 1600, and STOP for my confirmation before the full
  1600-run if the projected time exceeds 30 min.
- Write Alvaro/generated/regmi_syn_ds2_val.csv so I can submit it and read real
  ds2-val MRR under SyN (x3 rule), to compare against affine's 0.884.

Do Task A fully, then the Task B cost probe, then STOP for confirmation before the
long SyN run. Report: ds3 leak val file + bijection sanity, and the single-SyN
cost number.

---

yes

---

<task-notification>
<task-id>bynhewxrw</task-id>
<tool-use-id>toolu_01Pjx5SYvZ2PYVRV5otcvtvH</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-alvaro-Documents-GitHub-ehl-paris-2026-medical-retrieval/304770ec-6830-4b66-889b-2a4a221d4c19/tasks/bynhewxrw.output</output-file>
<status>completed</status>
<summary>Background command "Run SyN reg-MI on ds2 val (1600 registrations)" completed (exit code 0)</summary>
</task-notification>

---

Work ONLY inside Alvaro/. Build the FINAL combined submission using the best method
per dataset, now that all three are measured:
  ds1 -> plain_mi   (content, ~1.0)
  ds2 -> reg_mi, transform Affine   (0.884 real; SyN was worse at 0.846, do NOT use SyN)
  ds3 -> affine_argmin   (geometry leak, confirmed 0.333 = 1.0 x1/3)

Run: python -m Alvaro.src.make_submission --data-root . \
  --ranker-map "1:plain_mi,2:reg_mi,3:affine_argmin" --transform Affine \
  --out Alvaro/generated/final_submission.csv

This re-runs ds2 affine on test (~82 min) plus instant ds3 affine-argmin and ds1
plain MI. Print the upfront cost estimate and the per-pool bijection/sanity, assert
377 rows, then run in the background. Report the file when done. This is our final
leaderboard entry.

Also: fix the misleading cost line for future runs — PER_REG_SEC is hardcoded to
0.49 (affine); make it depend on transform (affine ~0.49s, SyN ~1.24s measured) so
the upfront estimate is correct. Small change, do it while ds2 runs.